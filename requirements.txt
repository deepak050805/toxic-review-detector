# PyTorch-only moderation stack — do NOT add tensorflow / tensorflow-cpu / tf-keras.

# Web API and server runtime
Flask==2.3.3
gunicorn==21.2.0

# Core ML (CPU inference)
numpy==1.26.4
torch==2.2.2
transformers==4.40.2
tokenizers==0.19.1
safetensors==0.4.5
huggingface-hub==0.23.5

# Utilities
requests==2.31.0
python-dotenv==1.0.0
